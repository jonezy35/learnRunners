--- 
- name: basic
  hosts: all
  gather_facts: false

  tasks:
    - name: whoami
      ansible.builtin.command: "ip a"
      register: iamwho

    - name: debug
      ansible.builtin.debug: 
        var: iamwho